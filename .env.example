# Database
# IMPORTANT: If your password contains special characters (!, @, #, $, %, etc.),
# you MUST URL-encode them. Use this JavaScript in your browser console:
#   encodeURIComponent("your-password-here")
#
# For Supabase, use the connection pooler (port 6543) to avoid "prepared statement already exists" errors
# Example: postgresql://postgres:encoded_password@db.abc123xyz.supabase.co:6543/postgres?pgbouncer=true
#
# Or for Vercel Postgres, use POSTGRES_PRISMA_URL instead
DATABASE_URL="postgresql://user:password@host:port/database?schema=public"

# Supabase
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Square
SQUARE_ACCESS_TOKEN="your_square_access_token"
NEXT_PUBLIC_SQUARE_APP_ID="your_square_app_id"
NEXT_PUBLIC_SQUARE_LOCATION_ID="your_square_location_id"
SQUARE_ENVIRONMENT="sandbox"
SQUARE_WEBHOOK_SIGNATURE_KEY="your_webhook_signature_key"

# App Configuration
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_BASE_PATH="/tiny-weddings"
NODE_ENV="development"

# Optional: Analytics
# NEXT_PUBLIC_GA_MEASUREMENT_ID="G-XXXXXXXXXX"

# Optional: Error Monitoring
# SENTRY_DSN="https://xxx@xxx.ingest.sentry.io/xxx"
